# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.coverage
htmlcov/

# Environnements virtuels
.venv/
.venv-*/
venv/
env/

# Django
*.sqlite3
db.sqlite3
staticfiles/
media/

# Variables d'environnement (jamais de secrets dans Git)
.env
.env.*
!.env.example
services/*/.env

# Node / Angular (frontend)
node_modules/
dist/
.angular/

# Kubernetes — secrets réels (garder uniquement les templates)
k8s/secrets/*.yaml
!k8s/secrets/*.template.yaml

# IDE / OS
.vscode/
.idea/
.DS_Store

# Claude Code — configuration locale, non partagée
.claude/

# Logs
*.log

# Clés JWT RS256 (générées via scripts/gen-jwt-keys.sh) — JAMAIS committées
**/keys/*.pem
**/keys/*.key

# CA + certificat mTLS gRPC interne (générés via scripts/generate-grpc-certs.sh)
# — secrets locaux, JAMAIS committés, même traitement que les clés JWT et .env.
/certs/

# Sauvegardes PostgreSQL locales (dumps) — jamais committées
/backups/

# PDF de factures générés à l'exécution — sortie de build, pas du code.
# Un test qui régénère un PDF ne doit pas produire un fichier à committer.
services/*/pdfs/
