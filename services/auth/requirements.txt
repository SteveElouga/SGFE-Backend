# Auth Service — requirements autonome (pas de -r vers la racine : chaque
# service doit pouvoir se builder/déployer indépendamment, notamment en Docker).
# Les versions communes sont alignées sur requirements.txt à la racine du repo,
# qui sert de référence — pensez à le consulter avant de bumper une version ici.

Django==5.0.6
djangorestframework==3.15.1
djangorestframework-simplejwt==5.3.1
grpcio==1.64.1
grpcio-tools==1.64.1
psycopg2-binary==2.9.9
django-environ==0.11.2
bcrypt==4.1.3
opentelemetry-api>=1.27.0
opentelemetry-sdk>=1.27.0
opentelemetry-instrumentation-django>=0.48b0
opentelemetry-exporter-otlp>=1.27.0
prometheus-client==0.20.0
