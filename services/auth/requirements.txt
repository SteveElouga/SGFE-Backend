# Auth Service — requirements autonome (chaque service doit pouvoir se
# builder/déployer indépendamment, notamment en Docker — pas de fichier
# de dépendances partagé entre services).

Django==5.2.17
djangorestframework==3.17.2
djangorestframework-simplejwt==5.5.1
cryptography==50.0.1
grpcio==1.81.1
grpcio-tools==1.81.1
protobuf==6.33.6
psycopg2-binary==2.9.9
django-environ==0.11.2
redis[hiredis]==5.2.1
requests==2.34.2
# Cron de purge RGPD (comptes/schedulers.py) — même version que les autres
# services du dépôt (paiement, campagne, facturation, notification, reporting).
APScheduler==3.10.4
opentelemetry-api==1.43.0
opentelemetry-sdk==1.43.0
opentelemetry-instrumentation-django==0.64b0
# `opentelemetry-bootstrap` ne détecte pas grpcio dans sa carte de détection
# pour ce service — constaté en conditions réelles (aucune trace produite sur
# les appels gRPC tant qu'il manquait), pas supposé à l'avance. Ce service ne
# sert que du gRPC (pas de WSGI) : instrumentation-django reste utile pour
# l'ORM/signals, mais ne couvre aucune requête entrante ici.
opentelemetry-instrumentation-grpc==0.64b0
opentelemetry-exporter-otlp==1.43.0
# opentelemetry-bootstrap (Dockerfile) : détecte psycopg2/redis/grpcio/requests
# déjà listés ci-dessus et installe leurs instrumenteurs — pas à lister ici.
opentelemetry-distro==0.64b0
python-json-logger==4.2.0
prometheus-client==0.20.0
pyroscope-io==1.2.2      # profiling continu — py-spy embarqué (cap_add SYS_PTRACE requis, voir docker-compose.yml)
