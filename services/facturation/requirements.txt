# Facturation Service — requirements autonome

Django==5.2.17
grpcio==1.81.1
grpcio-tools==1.81.1
protobuf==6.33.6
psycopg2-binary==2.9.9
django-environ==0.11.2
redis[hiredis]==5.2.1
PyJWT==2.13.0
APScheduler==3.10.4
weasyprint==69.0
opentelemetry-api==1.43.0
opentelemetry-sdk==1.43.0
opentelemetry-instrumentation-django==0.64b0
# `opentelemetry-bootstrap` ne détecte pas grpcio dans sa carte de détection —
# constaté en conditions réelles sur auth-service (aucune trace produite sur
# les appels gRPC entrants sans lui, voir services/auth/requirements.txt) ;
# même architecture gRPC ici, même correctif appliqué sans réexécuter le
# même test par service. `opentelemetry-instrumentation-django` couvre
# l'ORM/signals, mais ne couvre aucune requête entrante ici.
opentelemetry-instrumentation-grpc==0.64b0
opentelemetry-exporter-otlp==1.43.0
# opentelemetry-bootstrap (Dockerfile) : détecte psycopg2/redis/grpcio/requests
# déjà listés ci-dessus et installe leurs instrumenteurs — pas à lister ici.
opentelemetry-distro==0.64b0
python-json-logger==4.2.0
prometheus-client==0.20.0
